[package]
name = "main_app"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.41", features = ["serde"]}
diesel = { version = "2.2.10", features = ["sqlite", "chrono"] }
dotenvy = "0.15.7"
rocket = "0.5.1"
